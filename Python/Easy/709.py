
def toLowerCase(str):
    return str.lower()
    
# other method is to check char is uppercase, if so +32 in unicode so its lowercase and return it
string = "Hello"
lower = toLowerCase(string)
print(lower)
